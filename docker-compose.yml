services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cms-resume-frontend
    ports:
      - "6000:6000"
    environment:
      - NODE_ENV=production
      - PORT=6000
    env_file:
      - .env.production
    restart: always
